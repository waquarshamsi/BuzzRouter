# This is a basic Logstash configuration for receiving logs from the Spring Boot app.

input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

output {
  # For demonstration, we print to stdout. In a real setup, this would be an elasticsearch output.
  stdout { codec => rubydebug }
}